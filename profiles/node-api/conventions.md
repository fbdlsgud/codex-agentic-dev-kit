# Node API Profile

## Assumptions

- Backend source lives in `src/`.
- Keep route, service, and data access boundaries clear.
- Validate input at API boundaries.
- Avoid hardcoded secrets and unsafe SQL.
- Add tests for non-trivial behavior.

## Agent Notes

- Inspect existing framework conventions before adding routes.
- Record meaningful API work in `docs/ai/feature-log/`.

